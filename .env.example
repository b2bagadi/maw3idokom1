# Database
# For Vercel deployment with Prisma, you need TWO database URLs:
# 1. DATABASE_URL - Connection pooled URL (for runtime queries)
# 2. DIRECT_URL - Direct connection URL (for migrations)

# Example for Neon/Vercel Postgres with connection pooling:
# DATABASE_URL="postgresql://user:password@host/database?pgbouncer=true&connection_limit=1"
# DIRECT_URL="postgresql://user:password@host/database"

# Example for Railway Postgres:
# DATABASE_URL="postgresql://user:password@host:port/database"
# DIRECT_URL="postgresql://user:password@host:port/database"

DATABASE_URL="your_connection_pooled_database_url_here"
DIRECT_URL="your_direct_database_url_here"

# NextAuth
NEXTAUTH_SECRET="your_nextauth_secret_here_generate_with_openssl_rand_base64_32"
NEXTAUTH_URL="http://localhost:3000"

# WebSocket (Railway WebSocket Server)
NEXT_PUBLIC_WS_URL="ws://localhost:8080"
WS_SECRET="your_websocket_secret_here"

# Uploadthing (for file uploads)
UPLOADTHING_SECRET="your_uploadthing_secret"
UPLOADTHING_APP_ID="your_uploadthing_app_id"

# Optional: Email (if using email notifications)
# EMAIL_SERVER="smtp://user:password@smtp.example.com:587"
# EMAIL_FROM="noreply@yourdomain.com"

# Optional: Analytics
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
